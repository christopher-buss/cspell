// Small file for baseline performance testing
// This file contains minimal content to establish a performance baseline

const greeting = "Hello, World!";

function sayHello(name) {
    // Simple greeting function
    return `${greeting} Welcome, ${name}!`;
}

// Export the function
export default { sayHello };